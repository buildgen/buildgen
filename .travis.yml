language: cpp
compiler:
  - gcc
  - clang

before_install:
  - sudo apt-get install cmake
script:
 - pwd
 - SRC="$(pwd)"
 - echo $SRC
# - mkdir cmake/ && cd cmake/ && cmake .. && make install ; cd ..
# - mkdir bg/    && cd bg/    && gen -Ddebug -Dtest .. && make test ; cd ..
after-script:
# -  bg/buildgen-exe/test
